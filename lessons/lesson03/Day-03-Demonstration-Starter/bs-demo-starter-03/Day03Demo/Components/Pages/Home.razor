@page "/"

@* STARTER DOES NOT COMPILE YET — the instructor types two missing `private List<CartItem>`
   field declarations during today's demo (one in Products.razor, one in Cart.razor).
   The build errors you see when you first open this project are expected. *@

<PageTitle>Day 3 Demo - State Challenges</PageTitle>

<h1>Day 3 Demo: State Challenges</h1>

<p class="lead">
    This demo is pre-built except for two lines the instructor adds live: the
    <code>private List&lt;CartItem&gt; cartItems = [];</code> field in
    <strong>Products.razor</strong> and the identical field in <strong>Cart.razor</strong>.
    Once those are in place, navigate between Products and Cart to see the state-loss
    problem the Flux/Redux pattern (Day 4) solves.
</p>
