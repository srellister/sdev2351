@* ============================================================
    Practice 1: Wire this card to the Fluxor store.

    TODO Step 1: Add these three lines at the top of this file:
        @using Fluxor
        @inherits Fluxor.Blazor.Web.Components.FluxorComponent
        @inject IState<AccountState> AccountState

    TODO Step 2: Replace the "--" placeholder below with:
        @AccountState.Value.WishlistCount
   ============================================================ *@

<div class="card border-success h-100">
    <div class="card-header bg-success text-white">
        <h5 class="card-title mb-0">Wishlist</h5>
    </div>
    <div class="card-body text-center">
        <p class="display-4 text-success">
            <!-- TODO: Replace with @AccountState.Value.WishlistCount -->
            --
        </p>
        <p class="text-muted">saved items</p>
    </div>
</div>
