@* ============================================================
    Practice 1: Wire this card to the Fluxor store.

    TODO Step 1: Add these three lines at the top of this file:
        @using Fluxor
        @inherits Fluxor.Blazor.Web.Components.FluxorComponent
        @inject IState<AccountState> AccountState

    TODO Step 2: Replace the "--" placeholder below with:
        @AccountState.Value.MembershipTier
   ============================================================ *@

<div class="card border-info h-100">
    <div class="card-header bg-info text-white">
        <h5 class="card-title mb-0">Membership</h5>
    </div>
    <div class="card-body text-center">
        <p class="display-4 text-info">
            <!-- TODO: Replace with @AccountState.Value.MembershipTier -->
            --
        </p>
        <p class="text-muted">membership level</p>
    </div>
</div>
