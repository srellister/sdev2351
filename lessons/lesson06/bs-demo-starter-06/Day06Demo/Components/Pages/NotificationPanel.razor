@* ============================================================
    Demo A — Step 1 [CODE LIVE]: Wire this component to the Fluxor store.

    TODO Step 1: Add these three lines at the top of this file:
        @using Fluxor
        @inherits Fluxor.Blazor.Web.Components.FluxorComponent
        @inject IState<DashboardState> DashboardState

    TODO Step 2: Replace the "--" placeholder below with:
        @DashboardState.Value.NotificationCount

    TODO Step 3 [CODE LIVE]: Replace that bare binding with the three-state
        conditional block (@if IsLoading / else if ErrorMessage is not null /
        else) -- see Demo Script Step 3b. The finished reference ships this shape.
   ============================================================ *@

<div class="card border-primary h-100">
    <div class="card-header bg-primary text-white">
        <h5 class="card-title mb-0">Notifications</h5>
    </div>
    <div class="card-body text-center">
        <p class="display-4 text-primary">
            @* TODO: [CODE LIVE] Replace with @DashboardState.Value.NotificationCount *@
            --
        </p>
        <p class="text-muted">unread notifications</p>
    </div>
</div>
